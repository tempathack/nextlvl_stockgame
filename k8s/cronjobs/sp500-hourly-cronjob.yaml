apiVersion: batch/v1
kind: CronJob
metadata:
  name: sp500-hourly
  namespace: stock-game
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 2
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: sp500-hourly
            image: stock-game-backend:latest
            imagePullPolicy: Never
            command:
            - python
            - -m
            - app.tasks.sp500_hourly_fetch
            - --full
            envFrom:
            - configMapRef:
                name: stock-game-config
            - secretRef:
                name: stock-game-secrets
            resources:
              requests:
                memory: "256Mi"
                cpu: "200m"
              limits:
                memory: "512Mi"
                cpu: "500m"
          restartPolicy: OnFailure
